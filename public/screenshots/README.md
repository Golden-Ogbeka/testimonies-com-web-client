# PWA Screenshots

Place app screenshots here for the Web App Manifest `screenshots` field.

Required file (referenced in `src/app/manifest.ts`):

- `home.png` — 1280×720, desktop/wide screenshot of the home feed

These improve the install experience in Chrome on Android and desktop.
You can take screenshots manually or use a tool like Playwright or Puppeteer.
